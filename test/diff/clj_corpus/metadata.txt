(meta (with-meta [1 2] {:a 1}))
;;=> {:a 1}
(meta (with-meta {:x 1} {:m 9}))
;;=> {:m 9}
(meta (with-meta (list 1 2) {:a 1}))
;;=> {:a 1}
(meta (with-meta #{1 2} {:a 1}))
;;=> {:a 1}
(meta [1 2])
;;=> nil
(meta (vary-meta [1] assoc :b 2))
;;=> {:b 2}
(meta (vary-meta (with-meta [1] {:a 1}) assoc :b 2))
;;=> {:a 1, :b 2}
(meta (vary-meta [1] assoc :x 1 :y 2))
;;=> {:x 1, :y 2}
(meta (vary-meta (with-meta [1] {:a 5}) update :a inc))
;;=> {:a 6}
(:a (meta (with-meta [1] {:a 9})))
;;=> 9
(= (with-meta [1 2] {:a 1}) [1 2])
;;=> true
(with-meta [1 2] {:a 1})
;;=> [1 2]
(meta (vary-meta [1] (fn [m] (assoc m :k 1))))
;;=> {:k 1}
(meta (with-meta [1] nil))
;;=> nil
(meta (with-meta (with-meta [1] {:a 1}) {:b 2}))
;;=> {:b 2}
(count (meta (with-meta {} {:a 1 :b 2 :c 3})))
;;=> 3
(meta (with-meta (seq [1 2]) {:a 1}))
;;=> {:a 1}
