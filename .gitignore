# Zig build artifacts
zig-out/
.zig-cache/
zig-pkg/

# Nix build outputs
result
result-*

# direnv local cache
.direnv/

# macOS
.DS_Store

# Editor / IDE
*.swp
*~
.vscode/
.idea/

# Local-only docs and scratch (per-developer)
private/

# Claude Code local-only settings (per-developer)
.claude/settings.local.json

# Gate-cadence local state (scripts/check_gate_cadence.sh): the source-state
# fingerprint of the last full-gate pass + the additive batch counter.
.dev/.gate_pass
.dev/.gate_cadence
